# Environment Configuration
NODE_ENV=production

# SMTP Configuration (Hostinger Email)
# Get these from Hostinger hPanel -> Email -> Accounts -> Configure Email Client
# Common Hostinger SMTP settings:
#   - Host: smtp.hostinger.com OR mail.yourdomain.com
#   - Port: 465 (SSL) or 587 (TLS)
#   - Username: Your full email address (e.g., noreply@yourdomain.com)
#   - Password: Your email account password
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=info@blaupunkt-ev.com
SMTP_PASS=your-email-password-here

# Email Destination (where contact form emails are sent)
DESTINATION_EMAIL=info@blaupunkt-ev.com

# Server Configuration
PORT=5000

# Frontend URL (for CORS)
VITE_DOMAIN=https://blaupunkt-ev.com
