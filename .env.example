# Environment Variables Example
# Copy this file to .env for local development

# Note: With PHP backend, most environment variables are not needed
# The PHP API handles email configuration directly

# Optional: Backend API URL (only if using a different domain)
# VITE_API_URL=https://blaupunkt-ev.com
